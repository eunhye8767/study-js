/**
 *  # 어쩌구.d.ts
 * 
 *     ㄴ 타입정의 보관용파일
 *     ㄴ 다른 ts파일에서 import 가능
 *     ㄴ ts 파일에 타입 정의가 너무 길면 d.ts 파일 만들기도 함.
 */

// export type Age = number;
// export interface Person { name : string }

/**
 *  # type / common / test.d.ts
 */

type Age = number;